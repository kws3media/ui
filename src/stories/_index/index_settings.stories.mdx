import { Meta  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';

<Meta
  title="Framework Settings"
/>

## Framework Settings
Framework wide settings can be applied in one place.
Ideally in the entry file of your app.

### Usage
```js
import {applySettings} from '@kws3/ui';

applySettings({
  defaultIconFamily: 'fa',
  defaultToastPlacement: 'bottom',
  defaultSnackbarPlacement:'bottom-right',
  defaultNotificationPlacement:'top-right'
  hasTransitions: true,
  chartColors: [
    "#1b998b",
    "#d7263d",
    "#f46036",
    "#e2c044",
    "#c5d86d",
    "#03a9f4",
  ]
});

//you can also apply just one setting:
applySettings({
  hasTransitions: false
});
```

### Available Settings
<table class="table is-fullwidth">
  <tbody>
    <tr>
      <th>Setting Name</th>
      <th>Description</th>
      <th>Default Value</th>
    </tr>
    <tr>
      <th><code>defaultIconFamily</code></th>
      <td>Default icon family used by all Icon components</td>
      <td><code>"fa"</code></td>
    </tr>
    <tr>
      <th><code>defaultToastPlacement</code></th>
      <td>Default placement of toast notifications used by the Toast component.<br/>
      When <code>Toasts.create()</code> is used</td>
      <td><code>"bottom"</code></td>
    </tr>
    <tr>
      <th><code>defaultSnackbarPlacement</code></th>
      <td>Default placement of snackbar notifications used by the Snackbars component.<br/>
      When <code>Snackbar.create()</code> is used</td>
      <td><code>"bottom-right"</code></td>
    </tr>
    <tr>
      <th><code>defaultNotificationPlacement</code></th>
      <td>Default placement of floating notifications used by the Notifications component.<br/>
      When <code>Notifications.create()</code> is used</td>
      <td><code>"top-right"</code></td>
    </tr>
    <tr>
      <th><code>hasTransitions</code></th>
      <td>All components that have transitions, use this setting.<br/>
      When turned off, no transitions will take place</td>
      <td><code>true</code></td>
    </tr>
    <tr>
      <th><code>chartColors</code></th>
      <td>Default array of colors for chart visualisations shared by all charts.<br/>The order of colors matter, they will be used in sequence for each series of data.</td>
      <td><pre>[
"#1b998b", "#d7263d", "#f46036", 
"#e2c044", "#c5d86d", "#03a9f4"
          ]</pre>
      </td>
    </tr>
  </tbody>
</table>