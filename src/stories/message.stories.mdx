import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { Message } from "@kws3/ui";
import { action } from '@storybook/addon-actions';
import MessageDecorator from "./decorators/MessageDecorator.svelte";

export let args={
  size : "",
  color : "info",
  style : "",
  inner_style : "",
  classes : "",
  header_classes : "",
  inner_classes : "",
  title : "",
  has_title : false,
  dismissable : false
};

<Meta
  title="Helpers/Message"
  component={Message}
  args={args}
  argTypes={{
    color: {
      options: ["primary", "primary", "success", "danger", "warning", "dark", "light"],
      control: { type: 'select' }
    },
    size:{
      options:["small", "medium", "large"],
      control: {type:"select"},
    }
  }}
  decorators={[() => MessageDecorator]}
/>

<Description markdown={dedent`
# Message
## Message components for showing alerts.
`}/>

<Canvas>
  <Story
  name="Message"
  args={args}
  >
  {(args) => ({
    Component: Message,
    props: args,
    on: {
      close: action("Closed"),
      open: action("Opened")
    },
  })}
  </Story>
</Canvas>

<ArgsTable story="Message" />

### Example source code
<Source
  language='html'
  code={ dedent`
  <Message size="medium" color="info" >
    Your message here!!!
  </Message>`}
/>

### Usage
<Source
  language='javascript'
  code={ dedent`import { Message } from '@kws3/ui';`}
/>