import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { Modal } from "@kws3/ui";
import { action } from '@storybook/addon-actions';
import ModalDecorator from "./components/Modal.svelte";

export let args={
  size : "",
  is_active : false,
  closable:true,
  close_on_click_outside : true,
  style : "",
  inner_style : "",
  classes : "",
  inner_class : "",
  cy : ""
};

<Meta
  title="Helpers/Modal"
  component={Modal}
  args={args}
/>

<Description markdown={dedent`
# Modal
## Modal components for showing popups.
`}/>

export const Template = (args) => ({
  Component: ModalDecorator,
  props: args,
});

<Canvas>
  <Story
  name="Modal"
  args={args}
  >
  {(args) => ({
    Component: ModalDecorator,
    props: args,
    on: {
      click:action("Opened"),
    },
  })}
  </Story>
</Canvas>

<ArgsTable story="Modal" />

### Usage
<Source
  language='javascript'
  code={ dedent`import { Modal } from '@kws3/ui';`}
/>