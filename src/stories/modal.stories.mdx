import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { Modal } from "@kws3/ui";
import { action } from '@storybook/addon-actions';
import ModalDecorator from "./decorators/ModalDecorator.svelte";

export let args={
  size : "medium",
  is_active : false,
  closable:true,
  close_on_click_outside : true,
  style : "",
  inner_style : "",
  classes : "",
  inner_class : "",
  cy : ""
};

<Meta
  title="Helpers/Modal"
  component={Modal}
  args={args}
  argTypes={{
    size: {
      options: ["medium", "large"],
      control: { type: 'select' }
    }
  }}
/>

<Description markdown={dedent`
# Modal
## Modal components for showing popups.
`}/>

export const Template = (args) => ({
  Component: ModalDecorator,
  props: args,
});

<Canvas>
  <Story
  name="Modal"
  args={args}
  >
  {(args) => ({
    Component: ModalDecorator,
    props: args,
    on: {
      Click:action("Opened"),
    },
  })}
  </Story>
</Canvas>

<ArgsTable story="Modal" />

### Example source code
<Source
  language='html'
  code={ dedent`<Modal size="medium" is_active="true" >
      <h1>This is a Heading</h1>
      <p>Lorem ipsum.</p>
    </Modal>`}
/>

### Usage
<Source
  language='javascript'
  code={ dedent`import { Modal } from '@kws3/ui';`}
/>