import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { BarChart as Chart } from '@kws3/ui';
import { action } from '@storybook/addon-actions';
import BarChartDecorator from "./BarChart.svelte";

export let args={
  data: [
    [10, 20, 30, 90],
    [100, 200, 300, 900],
    [60, 50, 40, 30],
    [600, 500, 400, 300],
  ],
  labels: ["2021", "2022", "2023", "2024"],
  sets: [
    "Purchased",
    "Expense",
    "Sold",
    "Revenue",
  ],
  width: "100%",
  height: "400",
  colors: null,
  sparklines: false,
  horizontal: true,
  stacked: true,
  stacked_full_width: true,
  y_axis_options: {},
  options: {},
};

<Meta
  title="Charts/Bar Charts/Horizontal Stacked Full Width"
  component={Chart}
  args={args}
/>

<Description markdown={dedent`
# Bar Chart
`}/>

<Canvas>
  <Story
  name="Horizontal Stacked Full Width"
  args={args}
  >
  {(args) => ({
    Component: BarChartDecorator,
    props: args
  })}
  </Story>
</Canvas>

<ArgsTable story="Horizontal Stacked Full Width" />

### Usage
#### Import the component:
<Source
  language="javascript"
  code={
    dedent`import { BarChart } from '@kws3/ui';`
  }
/>

#### Import the SCSS:
<Source
  language="javascript"
  code={
    dedent`@import '@kws3/ui/styles/Chart.scss';`
  }
/>

#### SCSS customization variables with default values:
<Source
  language="css"
  code={
    dedent`
      $kws-chart-tooltip-background-color: $scheme-main;
      $kws-chart-tooltip-foreground-color: $scheme-invert-ter;
    `
  }
/>
