import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from "@storybook/addon-svelte-csf";
import dedent from 'ts-dedent';
import Version from '~/version.mdx';
import { RadialChart as Chart } from '@kws3/ui';
import { action } from '@storybook/addon-actions';
import RadialChartDecorator from "./RadialChart.svelte";
import * as MultipleStories from './multiple.stories';

export let args={
  data: [70, 50, 40, 20],
  labels: ["Bought", "Sold", "Donated", "Discarded"],
  width: "100%",
  height: "400",
  start_angle: 0,
  end_angle: 360,
  sparklines: false,
  colors: null,
  options: {},
};

<Version />

<Meta of={MultipleStories} />

<Description
  markdown={dedent`
# Radial Chart
`}
/>

<Canvas>
  <Story of={MultipleStories.Multiple} />
</Canvas>

<Controls />

### Usage

#### Import the component:

<Source
  code={
    dedent`import { RadialChart } from '@kws3/ui';`
  }
/>

#### Import the SCSS:

<Source
  code={
    dedent`@import '@kws3/ui/styles/Chart.scss';`
  }
/>

#### SCSS customization variables with default values:

<Source
  language="css"
  code={
    dedent`
      $kws-chart-tooltip-background-color: $scheme-main;
      $kws-chart-tooltip-foreground-color: $scheme-invert-ter;
    `
  }
/>
