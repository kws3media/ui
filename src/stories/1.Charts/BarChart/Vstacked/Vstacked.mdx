import { Story, Meta, Controls, Source, Description, Canvas  } from "@storybook/blocks";
import dedent from 'ts-dedent';
import Version from '~/version.mdx';
import * as Stories from "./Vstacked.stories.js";

<Version />

# Bar Chart

<Meta of={Stories} />
<Canvas of={Stories.Main} />
<Controls />

### Usage
#### Import the component:
<Source
  code={
    dedent`import { BarChart } from '@kws3/ui';`
  }
/>

#### Import the SCSS:
<Source
  code={
    dedent`@use '@kws3/ui/styles/Chart.scss';`
  }
/>

#### SCSS customization variables with default values:
<Source
  language="css"
  code={
    dedent`
      @use '@kws3/ui/styles/Chart.scss' with (
        $background-color: cv.getVar("scheme-main") !default;
        $foreground-color: cv.getVar("scheme-invert-ter") !default;
      );
    `
  }
/>
