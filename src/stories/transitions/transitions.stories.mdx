import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { Transition } from "@kws3/ui";
import TransitionDecorator from './Transitions.svelte';
import { action } from '@storybook/addon-actions';

export let args={
  x : 0,
  y : 20,
  from : 0.5,
  to : 1,
  transition : true,
  type : "fly",
  duration : 300,
  delay : 0,
  easing : "",
  class : ""
}

<Meta
  title="Standalone/Transitions"
  component={Transition}
  args={args}
  argTypes={{
    type:{control: 'select'}
  }}
/>

<Description markdown={dedent`
# Transitions
## A set of components to declaratively use Svelte transitions
`}/>

<Canvas>
  <Story
  name="Transitions"
  args={args}
  >
  {(args) => ({
    Component: TransitionDecorator,
    props: args,
  })}
  </Story>
</Canvas>

<ArgsTable story="Transitions" />

### Usage
##### Import the component:
<Source
  language='javascript'
  code={ dedent`import { Transition } from '@kws3/ui';`}
/>

### Examples
<div class="columns">
  <div class="column">
    Fly
    <Source
      language='jsx'
      code={ dedent`
        <Transition
          type="fly"
          duration="{300}"
          x="{0}"
          y="{20}"
        />`}
    />
  </div>
  <div class="column">
    Fade
    <Source
      language='jsx'
      code={ dedent`
        <Transition
          type="fade"
          duration="{300}"
        />
      `}
    />
  </div>
  <div class="column">
    Slide
    <Source
      language='jsx'
      code={ dedent`
        <Transition
          type="slide"
          duration="{300}"
        />
      `}
    />
  </div>
  <div class="column">
    Scale
    <Source
      language='jsx'
      code={ dedent`
        <Transition
          type="scale"
          duration="{300}"
          from="{0.5}"
          to="{1}"
        />
      `}
    />
  </div>
</div>