import {
  Story,
  Preview,
  Meta,
  ArgsTable,
  Source,
  Description,
  Canvas,
} from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import { SearchTool } from "@kws3/ui";
import SearchToolDecorator from "./SearchTool.svelte";
import { action } from "@storybook/addon-actions";
import playInteractions from "./searchtool.play.js";

export let args = {
  style: "",
  class: "",
  size: "",
  color: "",
  scoreThreshold: 5,
  disabled: false,
  readonly: false,
  placeholder: "Marvel characters...",
  highlighted_results: true,
  options: [],
  searchableKeys: [],
};

<Version />

<Meta
  title="Forms/SearchTool/SearchTool"
  component={SearchTool}
  args={args}
  argTypes={{
    color: {
      control: { type: "select" },
    },
    size: {
      control: { type: "select" },
    },
    search_strategy: {
      control: { type: "select" },
    },
  }}
/>

<Description
  markdown={dedent`
# SearchTool
## An Input component that supports search on multiple properties.
`}
/>

<Canvas>
  <Story name="SearchTool" args={args} play={playInteractions}>
    {(args) => ({
      Component: SearchToolDecorator,
      props: args,
    })}
  </Story>
</Canvas>

<ArgsTable story="SearchTool" />
<div class="notification is-primary is-light">
  <div class="columns">
    <div class="column">
      <h3>
        Input format <code>options</code>
      </h3>
      <p>
        Can send either an array of strings, or an array of objects.<br></br>Always
        ensure the shape of the array items are consistent.
      </p>
      <p>See supported data formats in example story above.</p>
      <p>
        Note: <code>option.label</code> contains matched characters wrapped
        inside <code>&lt;span class="h"&gt;</code> tags.{" "}
      </p>
      <p>
        Note: Highlighted results can be turned off by setting{" "}
        <code>highlighted_results</code> property to <code>false</code>
      </p>
    </div>
  </div>
</div>

### Usage

#### Import the component:

<Source
  language="javascript"
  code={dedent`import { SearchTool } from '@kws3/ui';`}
/>

#### Import the SCSS:

<Source
  language="javascript"
  code={dedent`@import '@kws3/ui/styles/SearchTool.scss';`}
/>

#### SCSS customization variables with default values:

<Source
  language="css"
  code={dedent`
      $kws-theme-colors: $colors !default;
      $kws-searchtool-radius: $radius !default;
      $kws-searchtool-border-color: $input-border-color !default;
      $kws-searchtool-box-shadow: 0 0.5em 1em -0.125em rgb(10 10 10 / 10%),
        0 0px 0 1px rgb(10 10 10 / 2%) !default;
      $kws-searchtool-focus-border-color: $input-focus-border-color !default;
      $kws-searchtool-focus-box-shadow-size: $input-focus-box-shadow-size !default;
      $kws-searchtool-focus-box-shadow-color: $input-focus-box-shadow-color !default;
      $kws-searchtool-disabled-background-color: $input-disabled-background-color !default;
      $kws-searchtool-disabled-border-color: $input-disabled-border-color !default;
      $kws-searchtool-disabled-color: $input-disabled-color !default;
      $kws-searchtool-selecting-color: $primary-invert !default;
      $kws-searchtool-selecting-background: $primary !default;
      $kws-searchtool-text-matches-color: currentColor !default;
      $kws-searchtool-text-matches-background: transparent !default;
      $kws-searchtool-text-matches-font-weight: $weight-bold !default;
    `}
/>
