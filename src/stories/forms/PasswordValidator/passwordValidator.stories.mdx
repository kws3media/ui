import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { PasswordValidator } from '@kws3/ui';
import PasswordValidatorDecorator from './PasswordValidator.svelte';
import { action } from '@storybook/addon-actions';

export let args={  
  password : "",
  is_valid : false
};

<Meta
  title="Forms/PasswordValidator"
  component={PasswordValidator}
  args={args}
/>

<Description markdown={dedent`
# Password Validator
`}/>

<Canvas>
  <Story
  name="PasswordValidator"
  args={args}
  >
  {(args) => ({
    Component: PasswordValidatorDecorator,
    props: args
  })}
  </Story>
</Canvas>

<ArgsTable story="PasswordValidator" />

### Usage
#### Import the component:
<Source
  language="javascript"
  code={
    dedent`import { PasswordValidator } from '@kws3/ui';`
  }
/>
