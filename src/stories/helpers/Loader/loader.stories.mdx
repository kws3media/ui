import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { Loader } from "@kws3/ui";
import { action } from '@storybook/addon-actions';

export let args={
  spinner_color: "grey",
  spinner_size: "small",
  background_color: "transparent",
  background_size: "small",
  style: "",
  is_inline: false,
  is_overlay: false
}

<Meta
  title="Helpers/Loader"
  component={Loader}
  args={args}
  argTypes={{
    spinner_color: {
      options: ['grey', 'light', 'warning', 'info', 'danger', 'primary', 'success', 'link'],
      control: 'select'
    },
    spinner_size: {
      options: ['small', 'medium', 'large'],
      control: 'select'
    },
    background_color: {
      options: ['transparent', 'warning', 'info', 'danger', 'primary', 'success', 'link'],
      control: 'select'
    },
    background_size: {
      options: ['small', 'medium', 'large', 'halfheight', 'fullheight'],
      control: 'select'
    },
  }}
/>

<Description markdown={dedent`
# Loader
## Graphic to display a loading status to users.
`}/>

<Canvas>
  <Story
  name="Loader"
  args={args}
  >
  {(args) => ({
    Component: Loader,
    props: args,
  })}
  </Story>
</Canvas>

<ArgsTable story="Loader" />


### Usage

##### Import the component:
<Source
  language='javascript'
  code={ dedent`import { Loader } from '@kws3/ui';`}
/>

##### Import the CSS:
<Source
  language='javascript'
  code={ dedent`@import 'path/to/node_modules/@kws3/ui/styles/Loader.scss';`}
/>

##### SCSS customization variables with default values:
<Source
  language='css'
  code={ dedent`
    $kws-loader-width: 2rem;
    $kws-loader-height: 2rem;
    $kws-loader-width-medium: ($kws-loader-width * 1.5);
    $kws-loader-height-medium: ($kws-loader-height * 1.5);
    $kws-loader-width-large: ($kws-loader-width * 3);
    $kws-loader-height-large: ($kws-loader-height * 3);
    $kws-loader-color: #ccc;
    $kws-loader-overlay-background: rgba(0,0,0,0.15);
  `}
/>