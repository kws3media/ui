import {
  Story,
  Preview,
  Meta,
  ArgsTable,
  Source,
  Description,
  Canvas,
} from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import { VirtualList } from "@kws3/ui";
import VirtualListDecorator from "./VirtualList.svelte";
import { action } from "@storybook/addon-actions";


export let args = {
  items: [],
  start: 0,
  end: 0,
  height: "600",
  item_height: null,
  class:"",
  end_threshold: 10,
};

<Version />

<Meta
  title="Helpers/VirtualList"
  component={VirtualList}
  args={args}
  argTypes={{}}
/>

<Description
  markdown={dedent`
# VirtualList
## A VirtualList component allow big amount data render with high performance and efficient. .
`}
/>

<Canvas>
  <Story name="VirtualList" args={args}>
    {(args) => ({
      Component: VirtualListDecorator,
      props: args,
      on: {
        end : action("End Readched")
      }
    })}
  </Story>
</Canvas>

<ArgsTable story="VirtualList" />

### Usage

##### Import the component:

<Source
  language="javascript"
  code={dedent`import { VirtualList } from '@kws3/ui';`}
/>
