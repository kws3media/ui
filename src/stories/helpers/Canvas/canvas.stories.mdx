import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { Canvas as KWS3Canvas } from "@kws3/ui";
import CanvasDecorator from "./Canvas.svelte";
import { action } from '@storybook/addon-actions';

export let args={
  styles: {
    border: "1px solid #b5b5b5",
  },
  width: "250px",
  height: "250px",
  lineWidth: 2,
  eraserWidth: 6,
  lineColor: "#ff0000",
  backgroundImage: "",
  readonly: false,
  disabled: false,
  image: "",
  expandFrom: "center center",
  initialScale: 1,
  expandedScale: 2,
  tools: ["Pen", "Eraser"],
  actions: ["controls", "colorpicker", "undo", "redo", "reset", "expand"],
  activeTool: "Pen",
  drawing_label: "",
  hide_colorpicker: false,
  cy: ""
};

<Meta
  title="Helpers/Canvas"
  component={KWS3Canvas}
  args={args}
  argTypes={{
    activeTool: {
      control: 'select'
    },
    tools: {
      control: 'check'
    },
    expandFrom: {
      control: 'select'
    }
  }}
/>

<Description markdown={dedent`
# Canvas
## Canvas components for drawing.
`}/>

<Canvas>
  <Story
  name="Canvas"
  args={args}
  >
  {(args) => ({
    Component: CanvasDecorator,
    props: args,
  })}
  </Story>
</Canvas>

<ArgsTable story="Canvas" />

### Usage
<Source
  language='javascript'
  code={ dedent`import { Canvas } from '@kws3/ui';`}
/>

##### Import the CSS:
<Source
  language='javascript'
  code={ dedent`@import '@kws3/ui/styles/Canvas.scss';`}
/>
