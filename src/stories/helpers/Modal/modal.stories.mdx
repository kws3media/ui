import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import Version from '~/version.mdx';
import { Modal } from "@kws3/ui";
import ModalDecorator from "./Modal.svelte";
import { action } from '@storybook/addon-actions';

export let args={
  size : "small",
  class : "",
  style : "",
  inner_class : "",
  inner_style : "",
  is_active : false,
  closable:true,
  close_on_click_outside : false,
  cy : ""
};


<Version />

<Meta
  title="Helpers/Modal"
  component={Modal}
  args={args}
  argTypes={{
    size: {
      control: 'select'
    }
  }}
/>

<Description markdown={dedent`
# Modal
## Modal components for showing popups.
`}/>

<Canvas>
  <Story
  name="Modal"
  args={args}
  >
  {(args) => ({
    Component: ModalDecorator,
    props: args,
    on: {
      click:action("Opened"),
    },
  })}
  </Story>
</Canvas>

<ArgsTable story="Modal" />

### Usage
<Source
  language='javascript'
  code={ dedent`import { Modal } from '@kws3/ui';`}
/>