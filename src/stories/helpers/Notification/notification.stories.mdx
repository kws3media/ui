import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { Notification } from "@kws3/ui";
import NotificationDecorator from './NotificationDecorator.svelte';
import { action } from '@storybook/addon-actions';

export let args={
  color : "info",
  style : "",
  light : false,
  dismissable : false
};

<Meta
  title="Helpers/Notification"
  component={Notification}
  args={args}
/>

<Description markdown={dedent`
# Notification
## Notification components.
`}/>

<Canvas>
  <Story
  name="Notification"
  args={args}
  >
  {(args) => ({
    Component: NotificationDecorator,
    props: args,
  })}
  </Story>
</Canvas>

<ArgsTable story="Notification" />

### Usage
<Source
  language='javascript'
  code={ dedent`import { Notification } from '@kws3/ui';`}
/>