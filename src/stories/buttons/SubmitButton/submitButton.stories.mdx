import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { SubmitButton } from '@kws3/ui';
import { action } from '@storybook/addon-actions';
import SubmitButtonDecorator from "./SubmitButton.svelte";

export let args={
  class : "",
  text : "Save Changes",
  icon : "save",
  icon_only : false,
  saved_text : "Saved",
  error_text : "Failed to Save",
  disabled : false,
  cy : "submit",
  tracker : {},
  color : "primary",
  size : "",
};

<Meta
  title="Buttons/Submit Button"
  component={SubmitButton}
  args={args}
  argTypes={{
    color: {
      control: 'select'
    },
    size: {
      control: 'select'
    }
  }}
/>

<Description markdown={dedent`
# SubmitButton
## Submit Button with built-in confirmation.
`}/>

<Canvas>
  <Story
  name="Submit Button"
  args={args}
  >
  {(args) => ({
    Component: SubmitButtonDecorator,
    props: args,
  })}
  </Story>
</Canvas>

<ArgsTable story="Submit Button" />

### Expects a tracker object to be sent from outside, in this format:
<div class="columns">
  <div class="column">
<Source
  language="javascript"
  code={
    dedent`
      {
        saving: false,
        saved: false,
        error: false
      }
    `
  }
/>
  </div>
  <div class="column">
    <p>This tracker object can be manipulated from outside to change the state of the button.</p>
    <p>For example, you can set tracker.saving = true before performing a network operation. And then set tracker.saved = true after the network request has completed.</p>
</div>

</div>

### Usage
#### Import the component:
<Source
  language="javascript"
  code={
    dedent`import { SubmitButton } from '@kws3/buttons';`
  }
/>
