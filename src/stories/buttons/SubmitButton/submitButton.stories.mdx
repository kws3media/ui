import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { SubmitButton } from '@kws3/ui';
import { action } from '@storybook/addon-actions';
import SubmitButtonDecorator from "./SubmitButton.svelte";

export let args={
  class : "",
  text : "Save Changes",
  icon : "save",
  icon_only : false,
  saved_text : "Saved",
  error_text : "Failed to Save",
  disabled : false,
  cy : "submit",
  tracker : {},
  color : "primary",
  size : "",
};

<Meta
  title="Buttons/Submit Button"
  component={SubmitButton}
  args={args}
  argTypes={{
    color: {
      control: 'select'
    },
    size: {
      control: 'select'
    }
  }}
/>

<Description markdown={dedent`
# SubmitButton
## Submit Button with built-in confirmation.
`}/>

<Canvas>
  <Story
  name="Submit Button"
  args={args}
  >
  {(args) => ({
    Component: SubmitButtonDecorator,
    props: args,
  })}
  </Story>
</Canvas>

<ArgsTable story="Submit Button" />

### Usage
#### Import the component:
<Source
  language="javascript"
  code={
    dedent`import { SubmitButton } from '@kws3/buttons';
    let BUTTON_SUBMIT;
    <SubmitButton bind:this={BUTTON_SUBMIT}
    //
    //Then we can use like
    BUTTON_SUBMIT.saving();
    BUTTON_SUBMIT.saved(() => {
      //some callback function
    }, 1000);
    BUTTON_SUBMIT.error(() => {
      //some callback function
    }, 1000);
    `
  }
/>
