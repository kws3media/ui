import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { DataSort } from '@kws3/ui';
import { action } from '@storybook/addon-actions';
import DataSortDecorator from "./DataSort.svelte";

export let args={  
  options : [
    {"name": "ID Newest First", "value": "id DESC"},
    {"name": "ID Oldest First", "value": "id ASC"}
  ],
  sort_by : ""
};

<Meta
  title="Datagrid/2. DataSort"
  component={DataSort}
  args={args}
  argTypes={{}}
/>

<Description markdown={dedent`
# DataSort
## DataSort component in datagrid.
`}/>

<Canvas>
  <Story
  name="2. DataSort"
  args={args}
  >
  {(args) => ({
    Component: DataSortDecorator,
    props: args
  })}
  </Story>
</Canvas>

<ArgsTable story="2. DataSort" />
