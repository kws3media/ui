import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { RangeSlider } from '@kws3/ui';
import { action } from '@storybook/addon-actions';

export let args={
  min : 0,
  max : 100,
  value : 0,
  class : "",
  style:"",
  size:"",
  color:"",
  output : true,
  disabled:false,
  circle:false,
  tooltip:false
};

<Meta
  title="Forms/Controls/Range Slider"
  component={RangeSlider}
  args={args}
  argTypes={{
    color: {
      control: 'select'
    },
    size: {
      control: 'select'
    }
  }}
/>

<Description markdown={dedent`
# Range Slider
## Custom Range Slider
`}/>

<Canvas>
  <Story
  name="Range Slider"
  args={args}
  >
  {(args) => ({
    Component: RangeSlider,
    props: args,
    on: {
      change : action("Changed")
    },
  })}
  </Story>
</Canvas>

<ArgsTable story="Range Slider" />

### Usage
#### Import the component:
<Source
  language="javascript"
  code={
    dedent`import { RangeSlider } from '@kws3/ui';`
  }
/>

#### Import the SCSS:
<Source
  language="javascript"
  code={
    dedent`@import 'path/to/node_modules/@kws3/ui/styles/RangeSlider.scss';`
  }
/>