import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import { RangeSlider } from '@kws3/ui';
import { action } from '@storybook/addon-actions';

export let args={
  min : 0,
  max : 100,
  step: 1,
  value : 0,
  class : "",
  style:"",
  size:"",
  color:"",
  output : true,
  disabled:false,
  circle:false,
  tooltip:false
};

<Meta
  title="Forms/Controls/Range Slider"
  component={RangeSlider}
  args={args}
  argTypes={{
    color: {
      control: 'select'
    },
    size: {
      control: 'select'
    }
  }}
/>

<Description markdown={dedent`
# Range Slider
## Custom Range Slider
`}/>

<Canvas>
  <Story
  name="Range Slider"
  args={args}
  >
  {(args) => ({
    Component: RangeSlider,
    props: args,
    on: {
      change : action("Changed")
    },
  })}
  </Story>
</Canvas>

<ArgsTable story="Range Slider" />

### Usage
#### Import the component:
<Source
  language="javascript"
  code={
    dedent`import { RangeSlider } from '@kws3/ui';`
  }
/>

#### Import the SCSS:
<Source
  language="javascript"
  code={
    dedent`@import '@kws3/ui/styles/RangeSlider.scss';`
  }
/>

#### SCSS customization variables with default values:
<Source
  language="css"
  code={
    dedent`
      $kws-slider-radius: $radius;
      $kws-slider-track-background: $grey-lighter;
      $kws-slider-track-radius: $radius;
      $kws-slider-track-border: 0px solid $grey;
      $kws-slider-track-shadow: 0px 0px 0px $grey;
      $kws-slider-thumb-background: $white;
      $kws-slider-thumb-radius: $radius;
      $kws-slider-thumb-border: 1px solid $grey-light;
      $kws-slider-thumb-shadow: none;
      $kws-slider-thumb-to-track-ratio: 2;
      $kws-slider-output-width: 3rem;
      $kws-slider-output-background: $grey-dark;
      $kws-slider-output-color: $white;
      $kws-slider-output-radius: $radius;
    `
  }
/>