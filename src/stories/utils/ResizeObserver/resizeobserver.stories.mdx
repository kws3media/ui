import { Meta, Story } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import ResizeObserver from "./ResizeObserver.svelte";

<Version />

<Meta title="Utils/2. Examples/ResizeObserver" />

### ResizeObserver

The `resizeObserver` is used to handle window resizing.

**Example:**

```html
{#if hasResizeObserver}
  <div use:resizeObserver on:resize={resizeHandler}>
{/if}

<script>
  import { hasResizeObserver, resizeObserver } from "@kws3/ui/resizeObserver";
</script>
```

<Story name="ResizeObserver" args={{}}>
  {(args) => ({
    Component: ResizeObserver,
  })}
</Story>
