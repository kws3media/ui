import { Meta, Story } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import ResizeObserver from "./examples/ResizeObserver.svelte";

<Version />

<Meta title="Utils/2. Examples/11. ResizeObserver" />

### ResizeObserver

The `resizeObserver` used to handle window resizing.

**Example:**

```js
import { hasResizeObserver, resizeObserver } from "@kws3/ui";
if(hasResizeObserver){
  <div use:resizeObserver on:resize={resizeHandler}>
}
```

<Story name="11. ResizeObserver" args={{}}>
  {(args) => ({
    Component: ResizeObserver,
  })}
</Story>
