import { Meta, Story } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import EnterKey from "./EnterKey.svelte";

<Version />

<Meta title="Utils/2. Examples/EnterKey" />

### isEnterKey

The `isEnterKey` function Detect whether a user has pressed Enter.

**Parameter:**
`Event`

**Example:**

```js
import { isEnterKey } from "@kws3/ui";
isEnterKey(event);
```

<Story name="EnterKey" args={{}}>
  {(args) => ({
    Component: EnterKey,
  })}
</Story>
