import { Meta, Story } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import CloneObject from "./examples/CloneObject.svelte";

<Version />

<Meta title="Utils/2. Examples/3. CloneObject" />

### CloneObject

Function used to clone js object to another.

```js
import { cloneObject } from "@kws3/ui";
let obj = {
  title: "Mr",
  name: "User",
};

$: clone = cloneObject(obj);
```

**Example:**

<Story name="3. CloneObject" args={{}}>
  {(args) => ({
    Component: CloneObject,
  })}
</Story>
