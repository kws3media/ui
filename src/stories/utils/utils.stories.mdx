import { Meta, LinkTo } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import {} from "@kws3/ui";

<Version />

<Meta title="Utils/1. Overview" />

## 1. truncate

The truncate() function shorten the given String to a specified length.

#### Usage

```js
import { truncate } from "@kws3/ui";
truncate(string, length);
```

## 2. nl_2_br

The nl_2_br function inserts HTML line breaks (`<br>`) in front of each newline (`\n`) in a string.

#### Usage

```js
import { nl_2_br } from "@kws3/ui";
nl_2_br(input);
```

## 3. cloneObject

The cloneObject function used to clone js object.

#### Usage

```js
import { cloneObject } from "@kws3/ui";
cloneObject(obj);
```

## 4. rAF

The rAF function - Request an animation before the next repaint.

#### Usage

```js
import { rAF } from "@kws3/ui";
rAF;
```

## 5. debounce

The debounce function is used to

#### Usage

```js
import { debounce } from "@kws3/ui";
debounce(callBackFunction, threshold);
```

## 6. capitaliseFirstLetter

The capitaliseFirstLetter function is used to capitalize the first letter of given string

#### Usage

```js
import { capitaliseFirstLetter } from "@kws3/ui";
capitaliseFirstLetter(string);
```

## 7. isEnterKey

The isEnterKey function checks the key stroke of `Enter`.

#### Usage

```js
import { isEnterKey } from "@kws3/ui";
isEnterKey(event);
```

## 8. isEscKey

The isEscKey function checks the key stroke of `Escape`.

#### Usage

```js
import { isEscKey } from "@kws3/ui";
isEscKey(event);
```

## 9. createDate

The createDate function makes mysql dates work in safari.

#### Usage

```js
import { createDate } from "@kws3/ui";
createDate(date);
```

## 10. Smart Forms

The `makeForm`, `makeForms` helpers used for tracking form data is dirty or not.

#### Usage

```js
import { makeForm, makeForms } from "@kws3/ui";
makeForm(data);
```

## 11. Resize Observer

The `resizeObserver` used to handle window resizing.

#### Usage

```js
import { hasResizeObserver, resizeObserver } from "@kws3/ui";
if(hasResizeObserver){
  <div use:resizeObserver on:resize={resizeHandler}>
}
```

## 12. scrollIntoActiveElelement

The `scrollIntoActiveElelement` used to scroll the list to active element.

#### Usage

```js
import { scrollIntoActiveElelement } from "@kws3/ui";
scrollIntoActiveElelement(listElement, activeElelement);
```

## 13. fuzzysearch

The `fuzzysearch` used to filter the object.

#### Usage

```js
import { fuzzysearch } from "@kws3/ui";
fuzzysearch(needle, haystack, opts);
```

## 14. keyboardEvents

The `keyboardEvents` used to trigger the events based on the key stroke.

#### Usage

```js
import { keyboardEvents } from "@kws3/ui";
keyboardEvents(node);
```
