import { Meta, Story } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import EscapeKey from "./EscapeKey.svelte";

<Version />

<Meta title="Utils/2. Examples/EscKey" />

### isEscKey

The `isEscKey` function used to Detect whether a user has pressed Escape key.

**Parameter:**
`Event`

**Example:**

```js
import { isEscKey } from "@kws3/ui";
isEscKey(event);
```

<Story name="EscKey" args={{}}>
  {(args) => ({
    Component: EscapeKey,
  })}
</Story>
