import { Meta, Story } from "@storybook/addon-svelte-csf";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import RandomInteger from "./RandomInteger.svelte";
import * as RandomIntegerStories from './randomInteger.stories';

<Version />

<Meta of={RandomIntegerStories} />

## randomIntegerFromInterval

The `randomIntegerFromInterval` returns a random integer.

**Parameters:**

1. min : `type: Number`
2. max : `type: Number`

#### Usage

```js
import { randomIntegerFromInterval } from "@kws3/ui/utils";
randomIntegerFromInterval(min, max);
```

<Story of={RandomIntegerStories.RandomInteger} />
