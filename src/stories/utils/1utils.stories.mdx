import { Meta, LinkTo } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";

<Version />

<Meta title="Utils/1. Overview" />

## 1. truncate

The truncate() function shorten the given String to a specified length.

#### Usage

```js
import { truncate } from "@kws3/ui/utils";
truncate(string, length);
```

## 2. nl2br

The nl2br function replace each newline (`\n`) with HTML line breaks (`<br>`) in a string.

#### Usage

```js
import { nl2br } from "@kws3/ui/utils";
nl2br(input);
```

## 3. cloneObject

The cloneObject function used to clone js object.

#### Usage

```js
import { cloneObject } from "@kws3/ui/utils";
cloneObject(obj);
```

## 4. rAF

The rAF function - Request an animation before the next repaint.

#### Usage

```js
import { rAF } from "@kws3/ui/utils";
rAF(callBackFunction);
```

## 5. debounce

The function that you want to execute after the debounce time.

#### Usage

```js
import { debounce } from "@kws3/ui/utils";
debounce(callBackFunction, threshold);
```

## 6. capitaliseFirstLetter

The `capitaliseFirstLetter` function converts the first character of a string to uppercase.

#### Usage

```js
import { capitaliseFirstLetter } from "@kws3/ui/utils";
capitaliseFirstLetter(string);
```

## 7. createDate

The createDate function makes mysql dates work in safari.

#### Usage

```js
import { createDate } from "@kws3/ui/utils";
createDate(date);
```

## 8. Smart Forms

The `makeForm`, `makeForms` helpers used for tracking form data is dirty or not.

#### Usage

```js
import { makeForm, makeForms } from "@kws3/ui/form";
makeForm(data);
```

## 9. Resize Observer

The `resizeObserver` used to handle window resizing.

#### Usage

```js
import { hasResizeObserver, resizeObserver } from "@kws3/ui/resizeObserver";
if(hasResizeObserver){
  <div use:resizeObserver on:resize={resizeHandler}>
}
```

## 10. Search

The `makeSearchEngine` is factory helper to create search engine with options.

#### Usage

```js
import { makeSearchEngine } from "@kws3/ui/search";
let searchOptions = {
  search_key, // optional, default 'value'
  scoreThreshold, // optional, default 5
  fuzzyOpts : {
    analyzeSubTerms, // Whether or not analyze sub-terms, default false
    analyzeSubTermDepth, //How many sub terms should be analyzed , default 10,
    highlighting: {
      before, // markup to add before mattched charectar, default '<em>'
      after, // markup to add after mattched charectar, default '</em>'
    },
  }
}
const fuzzysearch = makeSearchEngine(searchOptions);
fuzzysearch(needle, haystack);
```

## 11. keyboardEvents

The `keyboard` package used to trigger the events based on the key stroke.

#### Usage

```js
import { keyboardEvents } from "@kws3/ui/keyboard";
keyboardEvents(node);
```

## 12. makeKeyDefinition

The `makeKeyDefinition` is factory helper to create custom keydownHandler events.
Key combination can be made with any modifier keys from `Shift`, `Ctrl`, `Alt`, `Meta` with characters, numbers or function keys.

#### Usage

```js
import {makeKeyDefinition} from "@kws3/ui/keyboard";
const save = makeKeyDefinition(keyCombination) 
```

## 13. currency

The `currency` Returns currency format.

#### Usage

```js
import { currency } from "@kws3/ui/utils";
currency(number);
```

## 14. dateToOrdinal

The `dateToOrdinal` converts date to ordinal.

#### Usage

```js
import { dateToOrdinal } from "@kws3/ui/utils";
dateToOrdinal(number);
```

## 15. randomIntegerFromInterval

The `randomIntegerFromInterval` returns random integer.

#### Usage

```js
import { randomIntegerFromInterval } from "@kws3/ui/utils";
randomIntegerFromInterval(min, max);
```

## 16. randomPercent

The `randomPercent` returns random percentage. Default value for `min` and `max` are `1` & `100` respectively.

#### Usage

```js
import { randomPercent } from "@kws3/ui/utils";
randomPercent(min, max);
randomPercent(min);
randomPercent();
```

## 17. fileDownloader

Function used to download data/stream..

**Parameter:**
data: `type : object`
fileName: `type : string`

```js
import { fileDownloader } from "@kws3/ui/utils";
fileDownloader(data, filename);
```
