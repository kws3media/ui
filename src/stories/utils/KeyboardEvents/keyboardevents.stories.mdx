import { Meta, Story } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import KeyboardEvents from "./KeyboardEvents.svelte";

<Version />

<Meta title="Utils/2. Examples/KeyboardEvents/1. Pre-defined Events" />

### keyboard events

The `keyboard` package is used to trigger events based on any key stroke.

**Available Events:**
`enter`, `tab`, `escape`, `space`, `leftarrow`, `rightarrow`, `downarrow`, `uparrow`, `backspace`, `del`

**Usage Example:**

```js
<input use:event={customEvent} />
```

**Example:**

```js
<input type="text" use:enter={enterEvent} use:tab={tabEvent} />
<script>
  import { enter, tab } from "@kws3/ui/keyboard";
  function enterEvent() {
    console.log("Enter Pressed");
  }
</script>
```

<Story name="1. Pre-defined Events" args={{}}>
  {(args) => ({
    Component: KeyboardEvents,
  })}
</Story>
