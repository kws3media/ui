import { Meta, Story } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import Capitalize from "./examples/Capitalize.svelte";

<Version />

<Meta title="Utils/2. Examples/6. capitaliseFirstLetter" />

### capitaliseFirstLetter

The `capitaliseFirstLetter` function converts the first character of a string to uppercase.

**Example:**

```js
import { capitaliseFirstLetter } from "@kws3/ui";
capitaliseFirstLetter(string);
```

<Story name="6. capitaliseFirstLetter" args={{}}>
  {(args) => ({
    Component: Capitalize,
  })}
</Story>
