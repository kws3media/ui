import { Meta, Story } from "@storybook/addon-svelte-csf";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import DateToOrdinal from "./DateToOrdinal.svelte";
import * as DateToOrdinalStories from './dateToOrdinal.stories';

<Version />

<Meta of={DateToOrdinalStories} />

### DateToOrdinal

The `dateToOrdinal` function converts any date to an ordinal.

**Parameter:**
`Number`

**Example:**

```js
import { dateToOrdinal } from "@kws3/ui/utils";
dateToOrdinal(number);
```

<Story of={DateToOrdinalStories.DateToOrdinal} />
