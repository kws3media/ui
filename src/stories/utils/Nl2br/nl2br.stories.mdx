import { Meta, Story } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import Nl2br from "./Nl2br.svelte";

<Version />

<Meta title="Utils/2. Examples/Nl2br" />

### nl_2_br

The nl_2_br function inserts HTML line breaks (`<br>`) in front of each newline (`\n`) in a string.

**Parameter:**
`string`

```js
import { nl_2_br } from "@kws3/ui";
nl_2_br(text);
```

**Example:**

<Story name="Nl2br" args={{}}>
  {(args) => ({
    Component: Nl2br,
  })}
</Story>
