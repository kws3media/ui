import {
  Meta,
  Controls,
  Source,
  Canvas,
} from "@storybook/blocks";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import * as Stories from "./ScrollableList.stories";


<Version />

# ScrollableList
## ScrollableList component that efficiently displays large amounts of data

<Meta of={Stories} />
<Canvas of={Stories.Main} />
<Controls />

### Usage

##### Import the component:

<Source
  code={dedent`import { ScrollableList } from '@kws3/ui';`}
/>

<div className="columns">
<div className="column">
<div>Normal Usage Example:
<br/>You can define the <code>let</code> variables on main component</div>

```jsx
<ScrollableList
  {items}
  {height}
  {item_height}
  {end_threshold}
  {padding_threshold}
  bind:start
  bind:end
  on:end={endReached}
  let:item
  let:index
>
  <ListItem {item} {index} />
</ScrollableList>
```

</div>
<div className="column">
<div>Usage with loader slot Example:
<br/>You have to define the <code>let</code> variables in a slot wrapper</div>

```jsx
<ScrollableList
  {items}
  {height}
  {item_height}
  {end_threshold}
  {padding_threshold}
  bind:start
  bind:end
  on:end={endReached}
>
  <span
    slot="default"
    let:item
    let:index
  >
    <ListItem {item} {index} />
  </span>
  <span slot="loader">Loading...</span>
</ScrollableList>
```

</div>
</div>
