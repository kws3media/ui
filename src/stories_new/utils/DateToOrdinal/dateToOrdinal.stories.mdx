import { Meta, Story } from "@storybook/blocks";
import dedent from "ts-dedent";
import Version from "~/version.mdx";
import DateToOrdinal from "./DateToOrdinal.svelte";

<Version />

<Meta title="Utils/2. Examples/DateToOrdinal" />

### DateToOrdinal

The `dateToOrdinal` function converts any date to an ordinal.

**Parameter:**
`Number`

**Example:**

```js
import { dateToOrdinal } from "@kws3/ui/utils";
dateToOrdinal(number);
```

<Story name="DateToOrdinal" args={{}}>
  {(args) => ({
    Component: DateToOrdinal,
  })}
</Story>
