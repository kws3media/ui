import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import dedent from 'ts-dedent';
import { <%= section_name %> } from '@kws3/ui';
<% if (section_has_decorator) { %>import Decorator from "./_index/DefaultDecorator.svelte";<% } %>

export let args = {
};

<Meta
  title="<%= section_category %>/<%= section_name %>"
  component={<%= section_name %>}
  args={args}
  argTypes={{
  }}
/>

<Description markdown={dedent`
# <%= section_name %>
## <%= section_name %> component.
`}/>

<% if (section_has_decorator) { %>
export const Template = (args) => ({
  Component: Decorator,
  props: args
});
<% } %>

<Canvas>
  <Story
  name="<%= section_name %>"
  args={args}
  >
  {(args) => ({
    <% if (section_has_decorator) { if (section_decorator_has_slot) { %>Component: <%= section_name %>,<% } else { %>Component: Decorator,<% } } else { %>Component: <%= section_name %>,<% } %>
    props: args,
    on: {}
  })}
  </Story>
</Canvas>

<ArgsTable story="<%= section_name %>" />

### Usage
<Source
  language='javascript'
  code={ dedent`import { <%= section_name %> } from '@kws3/ui';`}
/>