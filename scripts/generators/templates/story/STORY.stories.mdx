import { Story, Preview, Meta, ArgsTable, Source, Description, Canvas  } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import dedent from 'ts-dedent';
import Version from '~/version.mdx';
import { <%= section_name %> } from '@kws3/ui';
<% if (section_has_decorator) { %>import <%= section_name %>Decorator from "./_index/DefaultDecorator.svelte";<% } %>

<Version />

export let args = {
};

<Meta
  title="<%= section_category %>/<%= section_name %>"
  component={<%= section_name %>}
  args={args}
  argTypes={{
  }}
/>

<Description markdown={dedent`
# <%= section_name %>
## <%= section_name %> component.
`}/>

<Canvas>
  <Story
  name="<%= section_name %>"
  args={args}
  >
  {(args) => ({
    Component: <% if (section_has_decorator) { %><%= section_name %>Decorator<% } else { %><%= section_name %><% } %>
    props: args,
    on: {}
  })}
  </Story>
</Canvas>

<ArgsTable story="<%= section_name %>" />

### Usage
<Source
  language='javascript'
  code={ dedent`import { <%= section_name %> } from '@kws3/ui';`}
/>